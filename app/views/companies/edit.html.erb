<div class="container py-4">
    <div class="d-flex justify-content-between mb-5">
    <%= link_to "< Retour",company_path(@company) , class: "text-secondary text-decoration-underline" %>
      </div>

        <div class="card shadow-sm border-0 mb-4 rounded-4">
          <div class="card-body p-4">
            <div class="d-flex align-items-center gap-3">

              <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center"
                  style="width:64px;height:64px;font-weight:700;font-size:1.25rem;">
                <%= @company.name.first(2).upcase %>
              </div>

      <%= form_with(model: @company, local: true,  url: company_path(@company), method: :patch) do |f| %>
              <div class="flex-grow-1">
                <h1 class="h3 mb-1"><%= f.text_field :name, class: "form-control" %></h1>
                <div class="text-muted"><%= @company.activity %></div>
              </div>
            </div>
          </div>
        </div>

        <div class="row g-3 mb-4">
          <div class="col-12 col-md-4">
            <div class="card shadow-sm rounded-4 h-100">
              <div class="card-body">
                <div class="small text-uppercase text-muted mb-2">Localisation 📍</div>
                <div class="fs-5"><%= f.text_field :location, class: "form-control" %></div>
              </div>
            </div>
          </div>
          <div class="col-12 col-md-4">
            <div class="card shadow-sm rounded-4 h-100">
              <div class="card-body">
                <div class="small text-uppercase text-muted mb-2">Taille 👥</div>
                <div class="fs-5"><%= f.select :size, options_for_select([["", ""], "1-10", "11-50", "51-200", "201-500", "500+"])%></div>
              </div>
            </div>
          </div>

          <div class="col-12 col-md-4">
            <div class="card shadow-sm rounded-4 h-100">
              <div class="card-body">
                <div class="small text-uppercase text-muted mb-2">Activité 🏭</div>
                <div class="fs-5"><%= f.text_field :activity, class: "form-control" %></div>
              </div>
            </div>
          </div>
        </div>

        <div class="row g-4">
          <div class="col-12 col-lg-6">
            <div class="card shadow-sm rounded-4 h-100">
              <div class="card-body">
                <h2 class="h5 mb-3">Description 📝</h2>
                <p class="mb-0 text-muted"><%= f.text_area :description, class: "form-control" %></p>
              </div>
            </div>
          </div>

          <div class="col-12 col-lg-6">
            <div class="card shadow-sm rounded-4 h-100">
              <div class="card-body">
                <h2 class="h5 mb-3">Objectifs 🎯</h2>
                <p class="mb-0 text-muted"><%= f.text_area :objectives, class: "form-control" %></p>
              </div>
            </div>
          </div>
        </div>

          <div class="d-flex justify-content-end gap-2 mt-4">
            <%= f.submit "Sauvegarder les changements", class: "btn btn-outline-secondary" %>
          </div>
      <% end %>
</div>
