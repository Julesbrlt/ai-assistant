<h1>Chats for <%= @company.name %></h1>
<div class="mb-4">
  <% if @chats.any? %>
    <ul>
      <% @chats.each do |chat| %>
        <li><%= link_to chat.title, chat_path(chat) %></li>
      <% end %>
    </ul>
  <% else %>
    There is no chats yet
  <% end %>
</div>

<%= simple_form_for [@company, @chat] do |f| %>
  <%= f.button :submit, "New chat", class: "btn btn-primary" %>
<% end %>
